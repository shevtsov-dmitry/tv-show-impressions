I have type
<type>
CREATE TABLE IF NOT EXISTS mood
(
    id                   INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    language_code        VARCHAR(2)  NOT NULL,
    state                VARCHAR(50) NOT NULL,
    age                  INT,
    coefficients         FLOAT      NOT NULL,
    displayed_by_default BOOLEAN
);
</type>

there is thing how it should be returned result ignoring id field:
<example>
          [
            #("language_code", json.string(mood.language_code)),
            #("state", json.string(mood.state),
            #("age", json.int(result.unwrap(mood.age, 0))),
            #("coefficients", json.float(mood.coefficients)),
            #("displayed_by_default", mood.displayed_by_default),
          ]
</example>

json supported wrappers: string, int, array, null, boolean, float, dict, nullable
